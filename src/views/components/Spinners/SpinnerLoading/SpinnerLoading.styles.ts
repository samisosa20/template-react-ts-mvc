// Packages
import styled from "styled-components"
import tw from "twin.macro"

export const StyledLoader = styled.div`
  ${tw`w-full bg-black opacity-50 h-screen fixed z-50 flex justify-center items-center top-0`}
  z-index:1400;
`
